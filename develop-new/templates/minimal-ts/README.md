# <Project Name>

Created with `develop-new` (DevLoop). This is a minimal TypeScript project ready for the DevLoop workflow.

## Commands

| Command | Script |
|---------|--------|
| Develop | `npm run dev` |
| Build | `npm run build` |
| Test | `npm test` |
| Typecheck | `npm run typecheck` |
| Lint | `npm run lint` (typecheck; add a linter in a later change) |

## DevLoop

- Project context lives in `devloop/` (see `devloop/context/CONTEXT.md`).
- Run `/develop <feature>` to drive the first feature through the full lifecycle.
- Run `/devloop-setup check` to verify the wiring.
